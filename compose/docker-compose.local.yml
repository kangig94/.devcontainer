# Local development overrides — auto-loaded by `make up-local`.
# Empty by default. Add machine-specific mounts here, e.g.:
#
# services:
#   lab:
#     volumes:
#       - ~/.gitconfig:/home/dev/.gitconfig

services:
  lab: {}
