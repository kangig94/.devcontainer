# Build configuration for Isaac Lab image
# Used by: make build-isaaclab

services:
  isaaclab:
    build:
      context: ../..
      dockerfile: .devcontainer/Dockerfile.isaaclab
      target: ${BUILD_TARGET:-user}
      network: host
      args:
        ISAACSIM_IMAGE: ${ISAACSIM_IMAGE:-nvcr.io/nvidia/isaac-sim:6.0.0-dev2}
        PYTHON_VERSION: ${PYTHON_VERSION:-3.12}
        ISAACLAB_VERSION: ${ISAACLAB_VERSION:-v3.0.0-beta}
